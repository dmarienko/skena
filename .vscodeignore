# - source & tooling — not needed at runtime (dist/ has everything compiled)
src/**
test/**
scripts/**
.vscode/**
.claude/**
esbuild.config.mjs
tsconfig.json
.eslintrc*
.gitignore

# - source maps — large, blocked by webview CSP anyway
**/*.map

# - dev deps / metadata
node_modules/**
.git/**

# - docs — README screenshots load from GitHub raw URLs; plans/specs are internal
docs/**
